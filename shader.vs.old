#version 120

attribute vec3 position;
attribute vec2 texture_positions;

varying vec2 texture_positions0;

void main(){
    gl_Position = vec4(position, 1.0);
    texture_positions0 = texture_positions;
}
